{
  "name": "fileclass-demo",
  "private": true,
  "type": "module",
  "description": "Demo-video tooling for Fileclass: stages a scenario's vault, then subtitles the take while the operator performs it.",
  "scripts": {
    "record": "node record.mjs",
    "voiceover": "node voiceover.mjs",
    "smoke": "node smoke.mjs",
    "publish": "node publish.mjs",
    "sync-docs": "node sync-docs.mjs"
  },
  "devDependencies": {
    "puppeteer-core": "^23.0.0"
  }
}
